--- Decorated AST ---
ProgramNode('TestExprError')
└── Block → tab[1], lev:1
    ├── Declarations
    │   └── VarDecl
    │       ├── VarDecl('x') → tab[39], type:integer, lev:1
    │       ├── VarDecl('y') → tab[40], type:string, lev:1
    │       └── VarDecl('b') → tab[41], type:boolean, lev:1
    └── CompoundStatement
        └── StatementList
            ├── Assign → type:integer
            │   ├── Var('x') → tab[39], type:integer
            │   └── IntLit('5') → type:integer
            ├── Assign → type:string
            │   ├── Var('y') → tab[40], type:string
            │   └── StrLit(''Arion'') → type:string
            ├── Assign → type:boolean
            │   ├── Var('b') → tab[41], type:boolean
            │   └── Var('true') → tab[33], type:boolean
            ├── Assign → type:integer
            │   ├── Var('x') → tab[39], type:integer
            │   └── BinOp op=plus → type:integer
            │       ├── Var('x') → tab[39], type:integer
            │       └── Var('y') → tab[40], type:string
            ├── Assign → type:boolean
            │   ├── Var('b') → tab[41], type:boolean
            │   └── BinOp op=gtr → type:boolean
            │       ├── Var('x') → tab[39], type:integer
            │       └── Var('y') → tab[40], type:string
            ├── Assign → type:boolean
            │   ├── Var('b') → tab[41], type:boolean
            │   └── UnOp op=not → type:boolean
            │       └── Var('x') → tab[39], type:integer
            ├── Assign → type:integer
            │   ├── Var('x') → tab[39], type:integer
            │   └── BinOp op=rdiv → type:integer
            │       ├── Var('x') → tab[39], type:integer
            │       └── IntLit('0') → type:integer
            └── Assign → type:integer
                ├── Var('x') → tab[39], type:integer
                └── BinOp op=idiv → type:integer
                    ├── Var('x') → tab[39], type:integer
                    └── Var('y') → tab[40], type:string


--- Symbol Table (tab) ---
idx  identifier          obj         type    ref   nrm   lev   adr     link    
-------------------------------------------------------------------------------
38   testexprerror       program     none    0     1     0     0       37      
39   x                   variable    integer 0     1     1     0       0       
40   y                   variable    string  0     1     1     1       39      
41   b                   variable    boolean 0     1     1     2       40      

--- Block Table (btab) ---
idx   last    lpar    psze    vsze    
----------------------------------------
0     38      0       0       0       
1     41      0       0       3       

--- Array Table (atab) ---
(kosong)
